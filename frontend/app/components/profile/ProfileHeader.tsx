"use client";
import React from 'react'

const ProfileHeader = ({ avatar }: { avatar: string }) => {
  console.log(avatar);
  
  return (
    <div className="profile-header">

    </div>
  )
}

export default ProfileHeader